# AGENTS.md

ChatGPT chat / workの切り替えは適宜Humanが判断するので、SystemUIで提案しないでください。

This repository is the writable workspace for an external architecture/design review of `eariver/japanese-generative-ai-survey`.

## Current task: Re:Phase 1

Human reset the reconstruction direction on 2026-09-15. Prior Phase structure/decisions/work order are historical evidence, not architecture authority. The operating-rule/live-status separation proposal is now complete through root design/connection review, one authorized independent bounded review, unit regression and bounded Git-aware integration. Candidate r2 remains unchanged. Reconstruction overall, production adoption and net lifecycle saving remain open/unproven.

Read first:

1. `handoff/rephase-1-continuation.md`
2. `outputs/rephase-1-integration-assessment.md` — current integration outcome, scope and remaining adoption conditions.
3. Necessary evidence from `notes/rephase-1-integration/README.md`. Candidate/review: `notes/rephase-1-connection/README.md`. Earlier runtime, review, connection and operating-contract assessments remain historical evidence. Broader direction: `outputs/rephase-1-direction-assessment.md`.

Historical reconstruct starting baseline: `ec6a502e9ae37e956d677f96f29af6c4e4591fc4`, not present HEAD. Human-fixed production baseline: `774dd39a951c9ac3818e83dfffd4c7666efb0a20`. Do not rebaseline or follow/inspect newer production main without explicit Human instruction. Retain the workspace and logically archive old Phases. Current Core/schema/role assignments are comparison starting points, not permanent architecture.

Human has lifted the Git prohibition. Necessary Git operations/Git-aware verification are authorized. Fixture objects/refs/commits must remain in an independent repository with an inert origin and no object-database sharing with reconstruct. Production mutation/posting/application/adoption still needs explicit Human decision. Ordinary final reconstruct commit/Pull/Push remain Human-owned; test authorization does not authorize publishing.

Current five-file candidate: `notes/rephase-1-connection/candidate.patch` (r2). It removes copied live status and its maintenance obligation from authority/index documents and the execution-record producer, while preserving substantive mandatory reading, review, session history, exact authority and Human boundaries. No candidate bytes changed during integration. The display probe is not a replacement Core validator and must not be installed wholesale.

Evidence now includes 42 unchanged normative sections, unchanged helper AST outside two templates, four document-only tests, five original mocked-loader unit tests, and the independent review with no actionable findings. The latest integration adds 27 distinct upstream bridge/Human Gate tests passed across 18 initial Linux successes plus 9 successful reruns after test-guard repairs, three real-loader CLI Profile scenarios (21 positive/negative command calls), and an unchanged-navigation Architecture revision/approval chain with active-approval absence rejection. No new authority mocks or Git-success stubs were used. Synthetic research/review/Human judgments are not real publication or approval evidence.

Keep failed setup attempts distinct: Windows hook argument handling, Windows fixed-Core backslash paths and missing tzdata, then Linux test-side check-ref-format/ls-tree allowlist omissions. Candidate/Core was not patched to clear these failures. Windows integration remains unestablished. Final Linux test controls report zero blocked operations. See Evidence logs; do not claim an initial clean 27/27 run.

Integration used a hash-verified 485-file fixed Core subset and synthetic Git commits, not the full production commit/history. Native Ubuntu fixture `/tmp/jgas-rephase-r2-502b3781` has its own database, inert origin and no alternates. Git review snapshot objects were created there, and test remote refs cleaned. Fixture HEAD/normal index and all captured code/doc bytes were preserved. Reconstruct HEAD observed during this unit was `388d827922459c2130512513148613657d0deede`; this is historical scoped evidence, not a perpetual current claim.

This candidate's bounded investigation is complete. Do not repeat successful tests/reviews absent a new concern. Next is Human's decision whether to advance r2 toward production application review. This is not ready-to-adopt certification: full upstream CI, actual Actions transport, actual adoption/contract compatibility, fixed-head seven-point audit, full Profile publication quality and net savings remain unproven. The earlier one-agent independent-review permission is completed and does not authorize another review. Do not weaken reviewed-commit validation, introduce a dashboard/manual framework/telemetry/resolver, or expand into unrelated repair work by default.

The old default sequence of Freeze repair followed by reader repair is superseded as reconstruction's work order. Their fixed-ref counterexamples remain maintenance evidence; fix a defect when it is a prerequisite for the particular route being evaluated. Do not make every maintenance item a prerequisite to architecture work. Do not transplant the whole old runtime patch over current #495/#496.

## Objective and evidence discipline

Minimize total lifecycle work across production, source research, all-role reasoning/review, repair/regeneration, CI/runtime, LLM usage, operational complexity, migration/dual maintenance, and Human handoff, while preserving publication quality, research sufficiency, provenance, fail-close for crisp invariants, Human authority, Weekly/Special viability and historical traceability. Moving work is not saving. Justified long-term investment is allowed.

Distinguish source inspection, function/schema witnesses, synthetic chains, actual production records, independent review and measured work. Full publication quality, current cold-start/general execution and net savings remain unproven. Do not infer cognitive work from artifact count or missing logs, or count necessary independent review as duplication. Unknown costs are not zero.

Current W33/W34/SP001 saved States say RELEASED; this reassessment did not repeat full dependency/publication validation. #492/#495/#496 are upstream achievements. Their merged implementation does not establish reconstruct adoption or ordinary-path viability. Preserve actual Human approval and historical authority.

Prior outputs, notes, handoffs, instructions, brief and patches stay in place as historical records. Their old current/next/phase wording does not override this entry or the current Human request. Do not overwrite historical decisions. Reuse relevant evidence with its scope and limitations rather than re-running successful tests or reloading all chats by default. Old 5-B non-support is not universal disproof; old lab failures are not successful publication. Read the original evidence when reusing a specific result.

## Repository and execution boundaries

- `eariver/japanese-generative-ai-survey` is READ ONLY unless Human explicitly authorizes a production mutation. No production branches, PRs, Issues/comments, State, decisions, Gates, Freeze/Release, adoption or migration changes.
- `eariver/reconstruct-japanese-generative-ai-survey` is writable for analysis, proposals and bounded isolated probes/candidates within the current request.
- Necessary Git operations and Git-aware tests are now authorized. Keep test commits/refs/objects in an independent isolated repository. No production change/adoption or main tracking is authorized; ordinary final reconstruct commit/Pull/Push remain Human-owned.
- New independent-agent work requires fresh explicit permission for the concrete scope. Prior Phase 4-C/5-B permissions are exhausted. One r2 independent review was explicitly authorized and completed with no actionable findings in scope; this is not general delegation permission.
- Do not fix or update a stale/dirty production checkout for investigation. Read fixed source through read-only APIs when needed.
- Git-aware fixtures are now allowed and must use an independent isolated Git root and inert origin. Never let fixture object/ref creation inherit reconstruct's Git database. Preserve the historical distinction between the old first run's 52 passes/6 Git-root errors and its isolated follow-up.

Choose targeted, cost-aware reads and stop when additional evidence is unlikely to change the decision. External tools/OSS are options when a concrete replacement boundary warrants comparison; do not conduct broad research merely for coverage. Production's current operating constraints remain effective until explicitly changed, even when reconstruct proposes alternatives.

For closeout-only requests, update the current handoff and stop. Do not infer authorization for a new phase or trial from a commit title.
