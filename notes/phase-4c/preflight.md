# Pre-authoring connection decision

2026-09-12, before semantic candidate generation. Protocol r1 scope retained.

The fixed production Draft Package contract requires `architecture_approval_sha256`, alongside admission/review bindings. Creating that approval is outside this trial. Do not insert a random hash, copy a production approval, or label a lab authorization as a Human decision.

Use full canonical Task/Card/View with local, explicitly lab-only screening decisions and exact bytes. Use canonical Selection `assignments`/`summary` and Draft Package `package`/`evidence_inputs`/`drafting_constraints` fields in **lab envelopes**; these envelopes are not complete canonical Selection or Draft Package artifacts. Draft Result uses its complete schema, with basis pointing to the exact lab package envelope and the unchanged canonical drafting prompt. Its schema conformance does not imply a canonical accepted package or admission. The local issue namespace is `lab-phase4c-weekly-2026-03-09`.

This records a connection limitation before generation, rather than claiming a full canonical production baseline. The observation can establish a canonical-field research/editorial slice and locate semantic work; **full canonical production execution remains untested**. No new meaning DSL or production engine is proposed. Local metadata assembly handles hashes/embedding only; root authors meanings and specific evidence/coverage references. Review covers the semantic chain as well as reader prose. Schema and exact named-reference checks cannot establish semantic sufficiency.

Contracts and two prompts are copied only from fixed main `005e59841272464307386abfc11f5b09228f0814`, with recorded Git blob and SHA-256 checks. Source text is a derived reading aid; raw bytes remain authoritative. Minimal checker calibration will reject a wrong subject reference and stale binding before authoring. Source discovery and question selection already occurred; they are not included in a claim that mechanical readiness preceded source exposure.

Cost observation: tool interface inspection exposes account quota but no role-token/billing meter. No account quota is converted to usage. Mixed root coordination/authoring/implementation time cannot be strictly separated; active time and tokens remain unknown. Observable stage timestamps, actions, files and reviewer work are retained; failed reviewer startup and resumption count as overhead. No telemetry framework is built.
